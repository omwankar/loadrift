/**
 * Product identity, in one place.
 *
 * The wordmark, the GitHub link, exported files and the browser storage
 * prefix all have to agree. Naming them here is what stops a rename from
 * leaving a stale title next to a still-Loadrift download.
 */
export const APP_NAME = 'Loadrift';
export const APP_SLUG = 'loadrift';
export const APP_TAGLINE = 'Build it, load it, watch it drift';
export const GITHUB_REPO = 'omwankar/loadrift';
export const GITHUB_URL = `https://github.com/${GITHUB_REPO}`;
